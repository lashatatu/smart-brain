const Navigation = () => {
  return (
    <nav style={{ display: "flex", justifyContent: "flex-end" }}>
      <p className={'f3 link dim black underline pointer'}>Sing Out</p>
    </nav>
  );
};

export default Navigation;
